#include <iostream>
#include <vector>

int main (void) {
    int input;
    std::vector<int> arr;
    std::cin.tie(NULL);
    std::cin >> input;

    while (input != 0) {
        int 
        std::vector.push_back();
    }
    return 0;
}