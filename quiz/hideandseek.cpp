#include <iostream>

int main (void) {
    int N, K;
    std::cin.tie(NULL);
    std::cin >> N >> K;

    // N can move N-1, N+1, N*2
    // find fastest way from N to K
    // 0 <= N, K <= 100000
    
    return 0;
}