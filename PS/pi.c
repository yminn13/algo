/*
 * from algospot.com
 * ID	:	PI
 * HINT	:	DP 
 */

#include <stdio.h>
#include <stdlib.h>

int main (void) {
	setbuf(stdout, NULL);

	int tc, i;
	int* num = NULL;

	scanf("%d", &tc);
	if (tc < 1 || tc > 50) return -1;

	num = (int*)mallloc(sizeof(int) * tc);
	for (i = 1; i <= tc; i++){
		
	}
}
