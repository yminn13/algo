/**
 * boj 15649
 */
#include <bits/stdc++.h>

void permutation (int start, int cnt) {
    for 
}

int main (void) {
    int N, M;
    std::cin >> N >> M;
    for (int i = 1; i <= N; i++) {
        permutation (i, M);
    }    
}